const PROJECT_BASE = '/' + window.location.pathname.split('/').filter(Boolean)[0];
const API_URL = window.location.origin + PROJECT_BASE + '/backend/api/';
const UPLOAD_URL = window.location.origin + PROJECT_BASE + '/backend/uploads/products/';
